@model Dictionary<string, object>
@{
    ViewBag.Title = "Home Page";

    List<Dictionary<string, string>> list1 = (List<Dictionary<string, string>>)Model["list1"];
    List<Dictionary<string, string>> list2 = (List<Dictionary<string, string>>)Model["list2"];
    List<Dictionary<string, string>> list3 = (List<Dictionary<string, string>>)Model["list3"];

}
<div class="row">
    <div class="col-md-6">
        <h3>Lista 1</h3>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Namn</th>
                    <th>År</th>
                    <th>Värde</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < list1.Count; i++)
                {
                    <tr>
                        <td>@list1[i]["name"]</td>
                        <td>@list1[i]["year"]</td>
                        <td>@list1[i]["pantone_value"]</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col-md-6">
        <h3>Lista 2</h3>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Namn</th>
                    <th>År</th>
                    <th>Värde</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < list2.Count; i++)
                {
                    <tr>
                        <td>@list2[i]["name"]</td>
                        <td>@list2[i]["year"]</td>
                        <td>@list2[i]["pantone_value"]</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
<div class="row">
    <div class="col-md-6">
        <h3>Lista 3</h3>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Namn</th>
                    <th>År</th>
                    <th>Värde</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < list3.Count; i++)
                {
                    <tr>
                        <td>@list3[i]["name"]</td>
                        <td>@list3[i]["year"]</td>
                        <td>@list3[i]["pantone_value"]</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
